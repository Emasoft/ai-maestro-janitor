---
trdd-id: c5da00c0-584d-4fb3-bbed-78c6fbc35c3a
title: Wikimem editor — SPLIT executor (skill + verify_split)
column: todo
created: 2026-06-18T19:35:24+0200
updated: 2026-06-18T19:35:24+0200
current-owner: janitor-session
assignee: janitor-session
priority: 3
task-type: feature
release-via: publish
parent-trdd: TRDD-54b25d7e
npt: [TRDD-b92a9dd0, TRDD-b4b9e27c, TRDD-b3eae1cd]
blocked-by: [TRDD-b92a9dd0, TRDD-b4b9e27c, TRDD-b3eae1cd]
relevant-rules: []
test-requirements: [unit]
---

# Wikimem editor — SPLIT executor (skill + verify_split)

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative; supersedes the body) — 2026-06-18

- **Current state:** authored, not started. This is TRDD-E (first executor, easiest,
  P2). BLOCKED until TRDD-A (txn core), TRDD-D (scheduler), and the NPT TRDD-b3eae1cd
  (librarian clustering precision) land.
- **NEXT ACTION:** create `skills/janitor-memory-split/SKILL.md` — a script lists
  pages > `split_max_bytes`; ONE opus agent per page produces overview + sub-pages,
  preserving type + ALL content, running THROUGH the TRDD-A transaction core.
- **Load-bearing facts (CRITICAL corrections from the plan):**
  - The executor mutates pages ONLY through the TRDD-A transaction core — it never
    writes a page directly.
  - **Backlink redirect is mandatory** (the biggest connectedness gap): on split,
    inbound links must repoint to the right sub-page IN THE SAME TXN; verify then
    asserts zero refs to retired slugs.
  - SPLIT recursion iterates **across heartbeat cycles**, NEVER as nested in-turn
    agent spawns (CC 2.1.181 caps sub-agent nesting at 5 levels) — one level per
    heartbeat.
  - `is_legal_split` refuses fragmentation / aspect-into-component etc.; a single
    note > cap is an un-splittable leaf → leave + surface (don't loop).
  - EXIT = verify-pass; bounded retry ≤3 → rollback-to-snapshot + surface + mutate
    nothing.
- **SUPERSEDED — do NOT carry forward:** none yet.
- **Durable artifacts to read before acting:** the plan
  `/Users/emanuelesabetta/.claude/plans/glittery-hatching-shell.md` (TRDD-E
  sub-section + the backlink-redirect + recursion-across-cycles corrections) and
  TRDD-54b25d7e.

## Scope

The first executor: a skill that splits an oversized page into a concise overview
plus type-preserving sub-pages, redirecting inbound links and partitioning globs,
all through the TRDD-A transaction core — plus its `verify_split` predicate.

## Key mechanisms

- `skills/janitor-memory-split/SKILL.md`: a script lists pages > `split_max_bytes`;
  ONE opus agent/page (one level per heartbeat; recursion across cycles, never
  nested in-turn — 5-level cap); overview (concise per-sub summaries + few notes) +
  sub-pages; preserve type + ALL content; **redirect inbound links**; rewrite each
  frontmatter (globs partition non-overlapping; tier/functionality per
  type-preservation); `is_legal_split` refusal; un-splittable-leaf escape (a single
  note > cap → leave + surface); depth cap + no-progress guard; EXIT/retry≤3 →
  rollback. Runs through TRDD-A.
- `verify_split` in `memory_edit_verify.py`.

## Acceptance

- An oversized fixture → overview + subpages, union == original, globs partitioned,
  type preserved, links redirected, converged.
- An un-splittable page → refused.
- A crash → resume restores.

## Dependencies

TRDD-A (txn core), TRDD-D (scheduler), and the NPT TRDD-b3eae1cd (librarian
clustering precision) — all NPT and blocked-by. See the plan ship order:
NPT → A → (B ∥ C) → D → E → F → G.
