---
trdd-id: d151fe52-28ce-4c69-b1ab-8f7505430f7c
title: memgrep — a markdown-AST-aware grepper + agent-memory helpers (Rust)
column: dev
created: 2026-06-06T18:07:35+0200
updated: 2026-06-06T18:35:00+0200
current-owner: janitor-dev-session
assignee: janitor-dev-session
priority: 1
severity: MEDIUM
effort: XL
labels: [memory-system, memgrep, rust, tooling, grep]
task-type: feature
parent-trdd: null
relevant-rules: []
release-via: none
delivery: direct-push
target-branch: main
test-requirements: [unit, integration]
runtime-targets: [macos, linux, windows]
implementation-commits: [0dfbbdd, ed68e8e, 9d030cb, 21d9bf5, eedaada]
last-test-result: pass
last-test-at: 2026-06-06T20:30:00+0200
external-refs: ["https://quarto.org/docs/authoring/markdown-basics.html#spans", "https://pandoc.org/MANUAL.html#extension-bracketed_spans"]
---

# TRDD-d151fe52 — memgrep

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-06-06

**What:** `memgrep` — a cross-platform, markdown-syntax-AWARE grep written from scratch in
Rust, plus agent-memory helpers. It is the query engine for the markdown memory system
(see the wiki/KB design — pure-markdown, git-tracked, one-fact-per-line, auto-`memory-index.md`).
It exists because plain `rg` is markdown-blind: it cannot exclude code blocks, scope to a
heading/chapter, range over heading numbering, match only bold/span-tagged text, or follow
the link/TOC graph — and ~90% of the requested features are AST-level, not line-regex.

**Architecture decision (made 2026-06-06):** FROM-SCRATCH Rust, NOT an `rg`-binary wrapper —
but built ON ripgrep's *library crates* so we inherit its speed + traversal:
- `ignore` — ripgrep's gitignore-aware parallel directory walker.
- `regex` — ripgrep's exact matching engine (RE2-style, linear-time).
- `comrak` — GFM CommonMark AST with `sourcepos` (byte+line/col spans), native wikilinks
  (`[[ ]]`), frontmatter delimiter, tables, task lists, strikethrough, footnotes, autolinks,
  math (`$`/`$$`), description lists, header IDs.
- `clap` (derive) — arg parser (we fully control the grammar).
- `serde` + `serde_yaml` — frontmatter field parsing.
A subprocess wrapper was REJECTED: the hard 90% (structural filters) needs the full AST
anyway, so rg-the-binary would only do the trivial final match while adding IPC; using rg's
*crates* gives the same speed with full structural control and no process boundary.

**Span metadata format (user-specified):** Pandoc/Quarto bracketed spans —
`[visible text]{.class key="security, backend"}`. memgrep recognizes these inline and indexes
the `.class` name(s) + the `key="..."` comma list as greppable metadata. A memory line may be
prefixed: `[summary]{.mem key="security, backend"} the fact sentence…`.

### Design constraints (USER, 2026-06-06 — load-bearing)
1. **LENIENT with every markdown flavour.** There are hundreds of MD flavours; memgrep must
   stay compatible by treating anything it does not understand as PLAIN TEXT, and must NEVER
   crash on an unknown syntax variation. Mechanics: CommonMark/comrak already degrades unknown
   constructs to text (no "syntax errors"); ADD per-file isolation (one bad/binary/non-UTF8
   file is skipped with a warning, never aborts the run — like `rg`) and a PLAIN-LINE-GREP
   FALLBACK so memgrep is ALWAYS ≥ `grep` regardless of flavour. All parsing wrapped so a
   panic in one file cannot kill the process.
2. **MINIMIZE the instructions needed to teach it.** The teaching-skill must be tiny. Achieve
   it by REUSING syntaxes Claude already knows from training — do NOT invent new ones. The
   unifying rules (the whole skill in ~4 sentences): (a) every matcher value is a REGEX, same
   as `grep`/`rg`; (b) any flag that has a `grep`/`rg` equivalent keeps that exact name+meaning
   (`-i -w -v -n -l -c -e -A -B -C -g`); (c) numeric/version ranges use PIP/PEP-440 syntax
   (`>=1.2,<3.5`); (d) wildcards are `*`; (e) different flags AND-narrow, comma-lists within a
   flag OR-widen. Be lenient on input too: accept `2`, `2..3`, `>=2,<=3` for ranges. Goal: a
   Claude can use memgrep correctly from `grep` muscle memory + these 5 rules.

### Build approach: tracer-bullet
Get a THIN end-to-end slice compiling + running first (ignore-walk → comrak-parse → regex →
`path:line:col`, with the leniency fallback), prove the whole dep stack + comrak API against
the COMPILER (do not assert the comrak API from memory — verify by building), THEN widen to the
structural flags. Forced verification: not "done" until `cargo test` is green + `cargo clippy`.

### ✅ PHASE 1 DONE + VERIFIED (commit 0dfbbdd) — 2026-06-06T18:35
Crate `tools/memgrep/` (comrak 0.52 + ignore 0.4 + regex 1.12 + clap 4.6, edition 2024).
Per-line block context from the AST → grep raw lines filtered by it. Shipped:
`PATTERN [PATH...]`, `-i -w -l -c`, `--json`; `--no-code`/`--code`/`--code-lang`; `--heading`
(bool scope; positional regex matches heading text); `--level` (`2`/`2..3`/`>=2` lenient);
`--in=RE` (chapter + sub-chapters). Leniency landed: binary skip (NUL probe), lossy UTF-8,
per-file isolation, `catch_unwind` around the parse → unknown flavours degrade to plain
line-grep, never crash. **10/10 e2e tests green** (tests/cli.rs vs tests/fixtures/sample.md),
**clippy clean**. Design fix found+fixed during build: `--heading` is a BOOL scope flag (not an
optional-value option) — the positional regex does heading-text matching — which removed a
clap optional-value-vs-path ambiguity AND is more grep-like (one matcher concept). A path
heuristic disambiguates structural-only `memgrep --heading FILE` (lone existing-path positional
→ treated as a path, not a regex).

### ✅ PHASE 2 DONE + VERIFIED (commit ed68e8e) — 2026-06-06T19:05
`--num` (prefix `1.2` = subtree · glob `1.2.*` = one level under · pip range `>=1.2,<3.5`,
comma=AND; numbers compare as version tuples), `--depth=N` (caps the enclosing section
number's component count), `--fm KEY=REGEX` (file-level frontmatter-field gate, repeatable,
AND). md.rs parses a leading dotted number per heading + a lenient frontmatter map +
`section_num(line)` (deepest numbered ancestor); search.rs adds `NumSpec` + per-line number
check + `frontmatter_ok()` file gate. All AND-compose with Phase 1. **16/16 e2e tests green**,
clippy clean. Verified on the real TRDD corpus: `--fm column=dev` → 2 TRDDs, `--fm
task-type=feature` → 4, combined `memgrep --no-code --fm task-type=feature -c` works.

### ✅ PHASE 3 DONE + VERIFIED (commit 9d030cb) — 2026-06-06T19:30
`--bold/--italic/--code-span/--strike=REGEX` (regex scoped to that inline markup, real start
column via sourcepos, kind-discriminated), `--class`(OR)/`--class-all`(AND) over a line's
bracketed-span `key="…"`, `--span-class` over the `.className`, `--list`/`--no-list`. md.rs now
collects inline spans (Strong/Emph/Strikethrough/Code with inner text) + list-item line ranges
+ a raw-line `]{…}` Pandoc-span recognizer (comrak strikethrough ext enabled). RefCell
double-borrow avoided (capture sourcepos → drop borrow → gather_inline_text). **20/20 e2e
tests green**, clippy clean. Proven: `--bold security` hits bold only; `--class security`/
`backend` hit the `[…]{.mem key="security, backend"}` line; `widget --no-list` = 0.

### ✅ PHASE 4 DONE + VERIFIED (commit 21d9bf5) — 2026-06-06T19:55
Per-line `node_kinds` u8 bitmask (table/quote/math/url/image/html/svg/footnote) from the AST
(Table, BlockQuote/MultilineBlockQuote, HtmlBlock/HtmlInline, FootnoteDefinition/Reference,
Math, Link/WikiLink=url, Image; SVG via raw-line `<svg` heuristic). `--node=a,b` (OR-any),
`--no-node` (exclude), per-kind sugar `--table/--quote/--math/--url/--image/--html/--svg/
--footnote`; `ref`/`note` alias footnote. comrak table+footnotes+autolink+math+multiline-quote
+wikilinks extensions enabled. **22/22 e2e tests green**, clippy clean.

### ✅ PHASE 5a DONE + VERIFIED (commit eedaada) — 2026-06-06T20:30
`memory.rs`: cross-file link graph (resolve `[text](path.md)` relative + `[[name]]` by stem;
external/broken/anchor classified; backlinks inverted). Subcommands (dispatched before grep
parsing; `-e` to grep a literal index/links/fact): **`memgrep index`** → regenerates
`memory-index.md` (per-note title+summary+tags+TOC+backlinks, atomic `--write`);
**`memgrep links --broken|--orphans|--to|--from`**; **`memgrep fact`** over the `<ISO> … :: `
shape filtered by `--cat/--comp/--session/--kind/--since/--until`, time-sorted. Enabled comrak
`front_matter_delimiter` → YAML no longer becomes Setext headings / shortcut links (fixed the
index TOC + link graph). **25/25 e2e tests green**, clippy clean (`--all-targets`).

### NEXT ACTIONS (post-compaction resume — memgrep is at `tools/memgrep/`, on `main`, UNPUSHED)
**Phase 5b (small):** grep-mode link filters `--links-to=NOTE`/`--linked-from=NOTE`/`--toc-follow=N`;
resolve `[[TRDD-<id8>]]` against the id8 segment in filenames (so TRDD cross-refs stop showing as
broken); wire `memgrep index` into the KB realtime-update path.

**Phase 6 (NEW — USER 2026-06-06, the BIG one): BOOLEAN COMPOSITION of conditions (AND/OR/NOT
+ grouping).** Today all filters AND-compose; the user wants full boolean logic. RECOMMENDED
model (capture + build): **`find`-style** — predicates are flags, operators are `-a`/`-and`
(implicit), `-o`/`-or`, `!`/`-not`, `( )` grouping (separate argv tokens), precedence NOT>AND>OR
(exactly `find`'s grammar, which matches the user's pseudo-CLI) — PLUS a `--where '<expr>'`
single-string infix DSL (`(path "*/memory/*.md" or path "*/archive/*.md") and not name "_*.md"
and fm.description "server*" and not code and heading.all "security"`). Both share ONE predicate
set + ONE expression-tree evaluator (And/Or/Not/Leaf). Architecture: the flat `search::Query`
(all-AND fields) becomes an EXPRESSION TREE of predicates; file-level predicates (path/name/fm)
eval per-file, line-level (code/heading/num/class/node/emphasis) per-line; a line matches iff the
tree is true with file-preds constant + line-preds per-line. NEW predicates to add: `-path`/`-name`
GLOBS with `**` (use the `globset` crate = ripgrep's glob engine, keeps the "ripgrep guts" theme);
`-fm KEY:MATCHER`; `-heading any:[…] all:[…] only:[…]`; a SMART `Matcher` value type that
auto-detects glob (`server*`) vs version-range (`>5.4.0`) vs regex. Alternative the user floated:
SQL-WHERE flavour — offer it as the `--where` dialect if preferred; find-style is the recommendation.
SQL maps less cleanly to glob/version/markdown predicates. S0 of Phase 6 built yet.

---
### (original Phase 5 plan — now split into 5a ✅ / 5b above)
Phase 5 (final) — link graph + memory helpers:
- `--links` (search link nodes), `--links-to=NOTE`/`--linked-from=NOTE` (out-links / backlinks
  across files — build a {file→out-links} map, invert for backlinks; resolve `[[wikilinks]]` +
  `[text](path.md)`).
- `--toc` (search index/TOC entries), `--toc-follow=N` (BFS the link graph to depth N, searching
  linked files' TOC descriptions).
- SUBCOMMANDS: `memgrep index [PATH]` → (re)generate `memory-index.md` (per-file 1-line summary +
  full heading TOC + tags + backlinks) — the <10ms realtime indexer the KB design needs;
  `memgrep links --broken|--orphans`; `memgrep fact [--cat/--comp/--session/--kind/--since/--until]`
  over the `^<ISO> … :: ` one-fact-per-line shape. This phase makes memgrep a clap multi-command
  (default = grep; `index`/`links`/`fact` subcommands). Then wire `index` into the KB update path.

## The arg grammar (committed — rg muscle-memory + pip version specifiers + CSS-ish selectors)

Base (ripgrep-compatible, so any Claude's `rg` habit transfers):
```
memgrep [FLAGS] PATTERN [PATH...]      # PATTERN is a regex (regex crate) applied to SURVIVING text
  -i smart/insensitive · -w word · -n line-nums(default on) · -l files-only · -c count
  -A/-B/-C N context · --json structured · --color · -. /--hidden · respects .gitignore via `ignore`
```

### Code blocks (feature 1–2)
- `--no-code` exclude all fenced+inline code · `--code` search ONLY code
- `--code-lang=python,rust` only those fenced langs (implies `--code`) · `--no-code-lang=bash`

### Frontmatter / metadata (feature 3) — composable, repeatable, AND-combined
- `--fm KEY=REGEX` (e.g. `--fm tags=security --fm verified='^2026-06'`) · `--fm-has KEY`

### Headings (feature 4–6)
- `--heading=REGEX` match heading text · `--level=2` or `--level=2..3` (`#`=1)
- `--in=REGEX` scope search to the section whose heading matches, INCLUDING its sub-sections
  · `--in-flat=REGEX` that heading's direct body only (no sub-sections)

### Heading numbering — pip/semver specifiers (feature 7–9)
- `--num=1.2` prefix · `--num='1.2.*'` wildcard · `--num='>=1.2,<3.5'` (comma = AND, pip-style)
  · operators `> >= < <= == !=`, ranges via comma
- `--depth=N` limit numbering depth (e.g. `--num=1.2 --depth=1` hits `1.2.*` but not `1.2.*.*`)

### Inline emphasis (feature 17 example) — regex applied ONLY inside that markup
- `--bold=REGEX` (**strong**) · `--italic=REGEX` (*em*) · `--code-span=REGEX` (`code`)
  · `--strike=REGEX` (~~del~~)

### Span-class metadata (feature 16) — Pandoc/Quarto bracketed spans
- `--class=security,backend` lines whose span `key="…"` contains ANY (OR) · `--class-all=…` ALL (AND)
- `--span-class=note` match by the `.className` itself (e.g. `{.note}`)

### Lists + GFM structures (feature 13–15)
- `--list` / `--no-list` (bulleted/ordered items)
- `--node=table,quote,math,url,image,html,svg,footnote,ref` restrict to those node kinds
  · `--no-node=…` exclude · individual sugar: `--table --quote --math --url --image --html --svg`

### Links / knowledge graph (feature 10)
- `--links` extract/search link nodes
- `--links-to=NOTE` what this note refers to (referred) · `--linked-from=NOTE` referrers (backlinks)

### TOC + autodiscovery recursion (feature 11–12)
- `--toc` search the index/TOC entries + their descriptions
- `--toc-follow=N` search the TOC descriptions of this file AND of linked files, recursion depth N

### Memory-helper subcommands (the "in addition" part)
- `memgrep index [PATH]` (re)generate `memory-index.md` (per-file summary + full TOC + tags +
  backlinks) — the <10ms realtime indexer the KB design calls for.
- `memgrep links --broken|--orphans` link-graph health.
- `memgrep fact [--cat=…] [--comp=…] [--session=ID] [--kind=…] [--since=DATE] [--until=DATE]`
  first-class query over the one-fact-per-line shape (`^<ISO-ts> … :: …`), time-sorted.

All filters AND-compose; the positional PATTERN is the final regex over whatever text survives
the structural filters. Example:
`memgrep security --num='>=1.2,<3.5' --no-code --class=backend memory/`
= "security" in chapters numbered 1.2.0–3.5.0, outside code, on lines span-tagged backend.

## Feature → implementation map (all = walk comrak AST → filter nodes → regex surviving text)
- code in/exclude, by-lang → `NodeValue::CodeBlock{info}` / `Code` filtering.
- headings/level/scope → `NodeValue::Heading{level}`; scope = node range between a heading and the
  next heading of ≤ its level (sub-sections = strictly greater level).
- numbering → parse a leading `N(.N)*` token from heading text; compare with a pip-style
  version-spec evaluator; depth = count of dotted components under the anchor.
- emphasis → `Strong`/`Emph`/`Code`/`Strikethrough` inline nodes → match within their text.
- spans → inline recognizer for `]{.class key="…"}` (Pandoc bracketed_spans), since comrak
  doesn't parse attributes natively; index `.class` + the `key` list.
- lists/tables/quotes/math/etc. → the corresponding `NodeValue` variants (GFM + math ext on).
- links/graph → `Link`/`Image`/wikilink nodes; build a {file → out-links} map, invert for
  backlinks; `--linked-from`/`--links-to` query it; `--toc-follow` BFS the link graph to depth N.
- output `path:line:col` via comrak `sourcepos`.

## Phases (each ≤ a focused build; commit per phase; tests per phase; NO push w/o user go)
1. **MVP core:** Cargo scaffold `tools/memgrep/`; `ignore` walk; comrak parse w/ sourcepos;
   `regex` match; `path:line:col` + `--json`/`-l`/`-i`/`-w`; `--no-code`/`--code`/`--code-lang`;
   `--heading`/`--level`/`--in`. (Already replaces rg for the top-value memory queries.)
2. **Numbering + composability:** `--num` pip ranges, `--depth`, `--fm` frontmatter filters, AND-compose.
3. **Inline + spans:** `--bold/--italic/--code-span/--strike`, `--class`/`--class-all`/`--span-class`, `--list/--no-list`.
4. **GFM structures:** `--node`/`--no-node` + sugar (table/quote/math/url/image/html/svg/footnote).
5. **Graph + memory helpers:** `--links`, `--links-to`/`--linked-from`, `--toc`/`--toc-follow`,
   and `memgrep index|links|fact` subcommands; wire `index` into the KB realtime-update path.

## Where it lives
`tools/memgrep/` Cargo crate in this repo (binary dropped on PATH). May graduate to its own
repo / `cargo install`-able crate later (open, minor decision). Cross-platform (macOS/Linux/
Windows) — pure Rust, no AppleScript/tmux.

## Why this TRDD exists
The user specced memgrep in detail as the query layer of the "memory system that works"
(replacing Claude Code's lossy file-memory). This TRDD is the durable, greppable spec so the
build survives compaction and sessions. Cross-referenced from the KB/wiki design.
