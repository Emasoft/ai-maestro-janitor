---
trdd-id: 56d24c02-b8f8-4a57-9c8b-f5f022fee3d8
title: A5 — hard-restart recovery rungs (relaunch / force_restart / resurrect)
column: todo
blocked-by: []
created: 2026-06-21T22:58:48+0200
updated: 2026-08-26T12:35:00+0200
implementation-commits: [fbfff71, eeb4aa8, 1d057f2, b684571, 9fb748a, 62cfa76, 9fd2c61, d09243d]
current-owner: claude
assignee: claude
priority: 3
severity: HIGH
effort: L
task-type: feature
parent-trdd: TRDD-324223a6
relevant-rules: [0, 1]
release-via: publish
test-requirements: [unit]
labels: [immortality, recovery, hard-restart, dangerous]
---

# A5 — hard-restart recovery rungs (TRDD-56d24c02)

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-06-21

**2026-07-08 — INCREMENT 2 WIRED (USER opted in "wire E2/E3" this date, see
`## Approval log`).** `daemon.task_session_liveness` now dispatches the hard rungs:
`fr.action_for(diagnosis, attempts, include_hard=True)` maps `dead`→`relaunch` and
extends the frozen ladder rearm→reload→update→`force_restart` (resurrect is
force_restart's no-channel FALLBACK, not an attempts rung); `sl.is_hard_rung`
routes those to `daemon._run_hard_restart` → `_hard_restart_plan` (build) →
`fleet_restart.fire_restart` (execute). EXECUTION requires BOTH the beat fire flag
AND the DEFAULT-OFF `CLAUDE_PLUGIN_OPTION_FLEET_HARD_RESTART_ENABLED=1`; otherwise
the built plan is DRY-RUN-logged — and a dry-run/fired hard attempt consumes the
same 4-attempt budget, so a disabled rung still walks to the crash-loop human
alert. `is_killable` is recomputed per attempt from the live Instance facts
(second gate under the diagnose_instance guarantee). Tests:
tests/test_daemon_hard_restart.py (6 beat-level, via the `fleet=` seam; never
enabled+killable) + test_fleet_recovery.py include_hard cases — 51/51 green with
the pre-existing fleet suites, ruff clean. **Production behavior is UNCHANGED
until the user sets the env flag** — everything dry-runs.

**Current state:** INCREMENTS 1+2 complete. Remaining before `testing`/`complete`:
a supervised live trial with the flag ON (user-present), then publish.

### 2026-07-29 — the rungs now restart IN PLACE; resurrect opens a tab, not a window

Owner directive, two parts, both landed (`62cfa76`, `9fd2c61`) — the card had no
`implementation-commits:` at all until today, so the backtracking field was empty for
seven commits; it is now populated from `git log -- scripts/lib/fleet_restart.py`.

1. **The relaunch is unattended-capable.** ⚠ **SUPERSEDED the same day — see the
   argv-mirroring section below. The hardcoded `_RELAUNCH_CMD` no longer exists.** The
   REQUIREMENT it encoded still holds and is now met by mirroring: a rung only ever fires
   at an ALREADY-unattended wedged session, so a relaunch that comes back up and then
   parks on a permission prompt has not recovered it — the process is running again (the
   scanner reads it healthy) while the session is as stuck as before.

2. **Restart in the ORIGINAL tab wherever possible.** Rungs 5/6 always did — they type
   into the existing pane. The gap was rung 7 firing too eagerly: `fleet_scan` resolves
   the terminal from the LIVE TTY and deliberately never from a recorded id, so a pane
   made unreachable by the **iTerm TCC denial already tracked in TRDD-VQ4LX7ND part 2**
   read as gone. `_hard_restart_plan` now retries `fleet_restart.recorded_terminal()` —
   the pane the session wrote at its own start — before escalating. FALLBACK, not
   substitute: live wins whenever it resolves, so a moved/recycled pane beats a stale
   recording. When rung 7 genuinely is last resort it now uses `tmux new-window -d`
   (a TAB) rather than `new-session`, because under iTerm2's tmux control mode a tmux
   SESSION surfaces as a WINDOW and a tmux WINDOW as a TAB; `new-session` remains the
   fallback when no session exists, since this rung must never fail to produce a plan.

The session id is passed to the builder as DATA — the first cut resolved it inside
`build_resurrect` and the suite's sandbox guard refused it, correctly: every `build_*` is
pure by contract so a plan stays inspectable/dry-runnable.

Safety posture UNCHANGED: `hard_restart_enabled()` is still DEFAULT-OFF, so none of this
can fire until the killing rungs are deliberately opted in. The invariants below still
hold verbatim.

### 2026-07-29 (later) — the relaunch MIRRORS the original argv; nothing is hardcoded

Landed in `d09243d`, shipped in **v0.65.0**. Supersedes §1 above.

**What forced it:** the 0.65.0 publish FAILED at CPV's security gate,
`CRITICAL=1 skillaudit:code_execution CLAUDE_CLI_PERMISSION_BYPASS` on the
`_RELAUNCH_CMD` literal. The scanner was RIGHT — the plugin was shipping a
permission-bypass invocation. Suppressing the rule or obfuscating the string would
have hidden a real capability while keeping it working: strictly worse than the
finding, and forbidden by both CPV's devitalizer contract and the janitor's own
security-agent contract.

**Owner directive:** don't spell claude's flags at all — read the argv the claude
executable was ACTUALLY launched with and replay that.

**The deeper bug the CPV finding exposed** (the important half): the hardcoded line
GUESSED two flags and DROPPED every other one. A real launch line carries `--model`,
`--mcp-config`, `--agent`, `--settings`, the user's own `--add-dir` set — so recovery
silently relaunched a wedged session as a DIFFERENT session.

`fleet_restart.relaunch_command(pid, project_root)`, most-authoritative first:
live argv (`ps -p … -o args=`, rungs 6/7) → the argv RECORDED at session start
(rung 5 — `dead` means the pid is already gone, so nothing else survives to read) →
`claude --continue`. VERIFIED against the live process: reproduces
`claude --add-dir /tmp --continue --dangerously-skip-permissions` verbatim. Any bypass
is now the USER'S, present only because they launched with it; the artifact ships none.

Three guards, each closing a way mirroring could go wrong:

- **`with_resume`** guarantees a resume flag. Most sessions launch as a bare `claude`,
  so a VERBATIM replay opens an EMPTY session in the recovered pane — and because the
  process is running, the scanner reads it HEALTHY while the whole transcript is gone.
  Silent total loss, worse than not recovering. An existing `--resume <id>` is left alone.
- **`argv_is_claude`** matches argv[0]'s BASENAME, never a substring: a line whose flags
  merely mention the word (`--add-dir ~/Code/claude-plugins`) is not a claude launch, and
  a substring test would let a recycled pid or a doctored `terminal-identity.json` turn
  recovery into arbitrary command execution in the user's own pane.
- **the command is DATA** to every `build_*`, exactly as `session` already is — resolving
  it inside a builder would fire `ps` from merely BUILDING a plan, including under
  `--dry-run`. Same purity contract the sandbox guard enforced on the tmux lookup.

`on-session-start.py` now records `argv` beside the pane identity in
`terminal-identity.json`. **This is the load-bearing half** — without it rung 5 has
nothing to mirror. Only the session can capture it, and only there: the hook's parent IS
the claude process (verified — `getppid()` resolves to `claude …`, not a shell wrapper).
It is recorded even when no pane resolved, because rung 7 needs no pane and would
otherwise lose the flags too.

Tripwire: `test_the_plugin_ships_no_permission_bypass_invocation` fails the suite if any
script spells a `claude … --dangerously-skip-permissions` invocation again. It matches
CPV's SHAPE, not any mention of the flag — banning the mention would forbid documenting
WHY this module mirrors, and that WHY is what stops the next author from adding it back.
It caught two files on its first run, one of which was a docstring that would have
re-tripped CPV.

**SUPERSEDED — do NOT carry forward:** `_RELAUNCH_CMD`, `_TEMP_DIRS`, `_temp_dirs()` and
the three tests that asserted their flag literals. Do not re-add a hardcoded relaunch
line, in any spelling.

**NEXT ACTION (unchanged, still the only blocker):** supervised live validation — set
`CLAUDE_PLUGIN_OPTION_FLEET_HARD_RESTART_ENABLED=1` on the daemon with the user
watching, freeze a sacrificial session, observe rungs 5/6 recover it IN ITS OWN TAB,
then decide default posture (stays opt-in per this TRDD's design). Note this needs the
daemon actually running — it is currently yielding to a live ai-maestro server that is
not executing its absorbed chores (ai-maestro#102).

### 2026-08-02 — moved `dev` → `blocked`; the stand-down is by DESIGN, verified in the log

The card had claimed `column: dev` since 2026-07-29 while nothing could touch it, which is
the failure `~/.claude/rules/the-kanban-is-a-pipeline-that-must-drain.md` names: a WORK
column asserts someone is working it *right now*, and an untrue column hides the stall from
the only view anyone checks. `pre-block-column: dev` records where to restore it.

The blocker is now verified first-hand rather than inferred. `daemon.log` shows every spawn
since 2026-08-01T03:35 doing start → activate keepalive → uninstall keepalive →
`stopping (server-owns-host)` within ~1 s, and `daemon.pid` is absent. **That is the
daemon behaving correctly**, not failing: a live ai-maestro server
(`~/.aimaestro/server-liveness.json`, beat 7 s old, `capabilities:[family-a,
singleton-chores]`) owns the host, so the janitor daemon stands down per TRDD-LU0C5KAR.
Do not "fix" this by forcing a daemon up — it would re-create the two-owner collision the
binary rule removed. It clears when ai-maestro#102 clears.

### ✅ 2026-08-22 — ai-maestro#102 is CLOSED. The block is discharged.

Verified via `gh issue view 102 --repo Emasoft/ai-maestro` → **CLOSED**. The condition this
card wrote down as its own release criterion is met, so `blocked-by` is now empty.

**Restored to `todo`, NOT to the recorded `pre-block-column: dev`, and the deviation is
deliberate.** `dev` asserts that someone is working the card RIGHT NOW; nobody is. The
universal-kanban rule says restore the pre-block column, but the drain rule says a WORK column
must be true, and "an untrue column is worse than an unstarted card". Where the two point
different ways the honest one wins — a mechanical restore would have put a stale card back into
the column that is hardest to audit.

**Note for whoever picks this up** (measured today on TRDD-AM8JD9SG's F7): the hard-restart
rungs are DEFAULT-OFF and read `False` on this host — `fleet_restart.hard_restart_enabled()` —
so `_run_hard_restart` dry-run-logs the plan it builds rather than executing it. Whatever this
card ships is inert until that opt-in is turned on, and AM8JD9SG's F7 constraint
(`hibernate`→`wake`, never `restart`, for a server-managed agent) must be satisfied BEFORE it
is.

Worth stating because the log is easy to misread: 21 h of silence plus a week-old
`daemon.spawn-attempt.ts` looks exactly like a dead daemon nobody respawns, and the honest
tell is the `stopping (server-owns-host)` line, not the gap.

So both gates on the NEXT ACTION are external: a running daemon (ai-maestro#102) and the
USER present for a supervised kill-and-restart trial. Neither is an agent's to unblock.

### ⏵ 2026-08-26 — gate 1 is SATISFIED (the daemon is up), and gate F7 got much sharper

**The daemon is running.** `daemon.pid` → 8368, alive with an uptime of 2 d 16 h, and
`daemon.log` shows `task 'session-liveness' starting / done` on the current beat. The
`stopping (server-owns-host)` stand-down described above is over; ai-maestro#102 closing was
not merely formal. So of the two external gates, only the USER-present supervised trial remains.

**But do NOT read that as "one step from ready", because F7's precondition is now measurably
UNMET — and today's evidence makes it the dangerous kind.** TRDD-AM8JD9SG's F11 was measured
this morning: `harness_backend.instance_is_server_owned` returns **False for every ai-maestro
agent on this host**, because all three of its signals are simultaneously dead (the CLI list
401s, `aimaestro-agent-roots.json` does not exist, and no agent workdir is under `~/agents/`).
The audit log shows the consequence: 275 recovery injections into ai-maestro agent roots since
2026-08-11.

**Compose that with this card and the stakes change category.** Today those 275 injections are
`rearm` (types `/janitor-arm`) and `esc_nudge` (sends ESC) — intrusive, out of compliance with
R42, and recoverable. With `CLAUDE_PLUGIN_OPTION_FLEET_HARD_RESTART_ENABLED=1` the same
mis-classification reaches `force_restart`, which is a **kill**. F7's constraint
(`hibernate`→`wake`, never `restart`, for a server-managed agent) cannot be honoured by code
that cannot tell a server-managed agent from any other session — and right now it cannot, on
this machine, for all 20 of them.

**So the supervised trial has a NEW prerequisite that is not the USER's to waive:** F11 must be
resolved first (either the exclusion widened, or ai-maestro#100's auth-free probe landed), and
the trial's sacrificial session must be one the daemon classifies correctly. Turning the flag on
before that is not "opt-in with a human watching" — it is arming a kill on a fleet the guard
cannot currently see.

The `is_killable` second gate and the `diagnose_instance` active-session guarantee both still
hold and are unaffected by F11; they protect the USER's working session, which is what they were
built for. Neither of them knows anything about agent ownership, so neither substitutes for the
missing classification.

**Load-bearing safety (the invariant the whole rung set hangs on):**
- NEVER kill the user's working session. `diagnose_instance` already guarantees a
  transcript-advancing (active) session is `healthy`, never `frozen`/`dead`, so it
  never reaches a hard-restart rung. `is_killable` is the SECOND gate: it refuses unless
  the pid's cmdline is a real `claude`, the instance is NOT `active`, the pid is not
  this process / the daemon, and the diagnosis is a genuinely-stuck one.
- `relaunch` types `claude --continue` (RESUMES the transcript — not total loss).
- DEFAULT-OFF: `CLAUDE_PLUGIN_OPTION_FLEET_HARD_RESTART_ENABLED` defaults false. Until the
  user opts in, every hard-restart rung is dry-run-logged, never executed.
- Bounded: the crash-loop guard (`session_liveness.crash_loop_tripped`) caps hard-restart
  attempts and pages a human instead of a restart storm.

## The rungs (already named in `session_liveness.RECOVERY_LADDER` 5-7)

- **relaunch (5)** — the pid is gone (`dead`) but the pane lives → type
  `claude --continue` into the pane to resume the session in place. No kill.
- **force_restart (6)** — the pid is alive but hard-wedged (`frozen`, gentle ladder
  exhausted) → `os.kill` the stuck pid, then `claude --continue` in the pane.
- **resurrect (7)** — the pane itself is unreachable → spawn a detached background
  `claude` (via `tmux new-session -d`) that kills + relaunches the stuck one. The
  "launch a background claude to kill+restart a stuck one" the user demanded.

## Increment 1 deliverable (this turn)

`scripts/lib/fleet_restart.py` — PURE plan-builders (`build_relaunch`,
`build_force_restart`, `build_resurrect`), the `is_killable` guard, the
`hard_restart_enabled()` opt-in, and `fire_restart(plan, *, enabled)` that EXECUTES only
when enabled (else returns a dry-run marker). Tests cover every builder, the
kill-guard's refusals (active session, non-claude pid, self/daemon pid), and the
default-OFF dry-run. NOT wired into the daemon (increment 2).

## Approval log

- 2026-07-08T00:32:00+0200 — APPROVED by USER (explicit "Wire hard-restart rungs
  (E2/E3)" selection via the session's AskUserQuestion, 2026-07-08). INCREMENT 2
  wiring authorized; execution remains behind the DEFAULT-OFF
  `CLAUDE_PLUGIN_OPTION_FLEET_HARD_RESTART_ENABLED` opt-in.

## Notes and lessons learned

[^1]: [ocd:2026-07-08 lmd:2026-07-08] `is_killable`'s claude-cmdline check is
  SUBSTRING-based — a command like `python3 not-claude.py` counts as a claude
  process. Caught when a gate-order test fixture named its decoy script
  "not-claude.py" and the rung FIRED instead of REFUSING. Lesson: test fixtures
  for a substring-matched deny-gate must not embed the matched token anywhere,
  and belt-and-suspenders the fixture (pid=0) so a gate regression still cannot
  reach `os.kill`.
