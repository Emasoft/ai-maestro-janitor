---
trdd-id: 8P4BNY5J
title: Route the model-fallback no-headroom keystrokes through pane_actuate with Event NO_HEADROOM
column: testing
created: 2026-09-16T23:06:20+0200
updated: 2026-09-17T08:33:29+0200
current-owner: session
created-by: session
task-type: bugfix
min-approval-requirement: none
assignee: session
mandate: true
mandated-by: user
approved: true
approval-judge: session
approval-datetime: 2026-09-16T23:06:20+0200
parent-trdd: N954KWUC
derived: true
priority: critical
npt: []
review-after: 2026-09-24
implementation-commits: [9c5c8fb2]
---

# Route the model-fallback no-headroom keystrokes through pane_actuate with Event NO_HEADROOM

EHT of TRDD-N954KWUC (its title: ONE screen-state reader drives EVERY keystroke the janitor types). Traced 2026-09-16 (/Users/emanuelesabetta/Code/AI-MAESTRO-JANITOR/ai-maestro-janitor/reports/board-drain/20260916_230459+0200-N954KWUC-no-headroom-path.md): the no-headroom fallback in scripts/detectors/model-fallback.py reaches the pane directly through terminal_trigger.send_verified / send_model_switch_true_error, bypassing pane_actuate and pane_policy entirely; Event.NO_HEADROOM has a well-formed policy row and zero callers. N954KWUC's box 2 holds literally (no fleet_inject.fire call site bypasses) but the card's own invariant does not. Fix: make model-fallback's keystrokes go through pane_actuate with Event.NO_HEADROOM so the reader/policy gates them (retry_wedge + NO_HEADROOM row) and pane-policy.log records the episode; keep the ESC-flush-then-/model-opus-then-confirm sequence 3T9HQEQ6 landed (fb25366f, 1533ccc9, both in v3.5.5). Related: TRDD-M4HVFU2A (the same detector typing /model opus into every pane). Acceptance: (1) model-fallback has no direct send_verified/send_model_switch_true_error call; (2) a test feeds a no-headroom frame + Event.NO_HEADROOM through the real policy and asserts the exact keystroke plan; (3) a live no-headroom episode appears in pane-policy.log (closes N954KWUC box 4's second half); (4) ruff/mypy/pyright clean.

## Approval log

- 2026-09-16T23:06:20+0200 — MANDATE issued by session (min-approval-requirement: none). Pre-approved: issuer authority >= required approver. No approval request was sent.
- 2026-09-16T23:09:20+0200 — scope precision (review 2026-09-16): the routed path MUST preserve the flush→/model opus→confirm sequence TRDD-3T9HQEQ6 landed (fb25366f, 1533ccc9); the NO_HEADROOM policy row's plan was not read tonight — if it plans a single /model opus, use or add the row that reproduces 3T9HQEQ6's sequence rather than regress it. Acceptance items 1–2 are to be read with that constraint. Sequenced after M4HVFU2A (npt).
- 2026-09-17T05:57:29+0200 — column → testing by session. code+tests landed, ruff/mypy/pyright clean, 67/67 tests pass; box 3 (live pane-policy.log episode) is a live-observation item left open

## ⏵ STATE — READ THIS FIRST ON RESUME

2026-09-17T05:57:23+0200 — DONE (code+tests). scripts/detectors/model-fallback.py: removed the two direct terminal_trigger.send_verified/send_model_switch_true_error calls and the true_error/session_liveness wedge classification; the keystrokes now go through pane_actuate.act(_actuate_terminal(terminal), Event.NO_HEADROOM, project_dir=str(state.project_root()), log=...). Added _actuate_terminal() to convert _this_terminal()'s {kind,pane|session_id} shape to the {tmux_pane|iterm_session_id} shape pane_state/pane_actuate/fleet_inject expect (two terminal-dict conventions coexist in this codebase; read-only helpers read_pane_text/parse_pane_model/confirm_model_switch keep using the old shape, unaffected). Acceptance (1) verified: grep for send_verified/send_model_switch_true_error in the file now matches only the explanatory comment, zero calls. Acceptance (2) already held by the pre-existing, unmocked tests/test_pane_policy.py::test_retry_wedge_no_headroom_flushes_then_switches_model_and_confirms (real pane_policy.plan(state, Event.NO_HEADROOM) on a real wedge fixture asserts steps == [ESC(FIELD_EMPTY), /model opus(MENU_SHOWN), Enter(IDLE_OR_WORKING)] -- 3T9HQEQ6's sequence). Acceptance (4) verified: ruff/mypy/pyright clean on scripts/detectors/model-fallback.py and tests/test_model_fallback.py. Acceptance (3) (a live no-headroom episode in pane-policy.log) is a live-observation box, left OPEN -- cannot be forced, needs a real spent-model-window wall (same caveat N954KWUC's own box 4 second half carries). tests/test_model_fallback.py rewritten: _wire_acting_detector now spies on pane_actuate.act (Event.NO_HEADROOM, terminal={tmux_pane:...}) instead of the two removed functions; replaced test_a_true_error_pane_routes_to_the_owner_ratified_sequence + test_an_idle_pane_keeps_the_esc_first_sequence with test_no_headroom_routes_through_pane_actuate_not_terminal_trigger_directly (both old functions monkeypatched to raise, proving main() never reaches them); updated the sibling/decline tests' assertions from called[verified]/called[true_error] to called[act]. 67/67 passed (test_model_fallback.py + test_pane_policy.py). N954KWUC/UA4FAX67 NOT touched. Report: reports/board-drain/20260917_impl-8P4BNY5J.md
2026-09-17T05:59:34+0200 — REVIEW FINDING (adversarial fork, not yet actioned): routing NO_HEADROOM through pane_actuate is CORRECT for the retry_wedge case (test_retry_wedge_no_headroom_flushes_then_switches_model_and_confirms pins it), but pane_policy's NO_HEADROOM row is wedge-ONLY by original N954KWUC/Phase-2 design (its docstring names only send_model_switch_true_error as the row it replaces, never send_verified). The OLD model-fallback code also typed via send_verified(esc_first=True) on a non-wedge (idle/working) pane still on the exhausted model -- that path is now a SILENT NO-OP under pane_policy, since _at_idle/_at_working have no NO_HEADROOM case. Given token_burn.model_fallback_verdict fires on usage-percentage (not on-screen error state), a spent window with an otherwise-idle pane (no visible wedge yet) is a plausible, maybe common, trigger -- so this may reintroduce the exact stalled-session failure mode the detector exists to prevent. NOT fixed here: fixing it means adding an idle/working NO_HEADROOM row to pane_policy.py, which is N954KWUC's file and this unit's assignment said not to touch N954KWUC. Recommend: the orchestrator/N954KWUC owner decide whether to (a) accept the narrowed wedge-only trigger as intentional, or (b) file a follow-up TRDD to add the missing row before this is truly done. See reports/board-drain/20260917_impl-8P4BNY5J.md for full detail.
2026-09-17T06:08:31+02:00 — FOLLOW-UP DONE. Orchestrator ruling: the wedge-only NO_HEADROOM row was a regression (pre-8P4BNY5J typed the switch on any readable non-wedge pane, not just retry_wedge). Extended scripts/lib/pane_policy.py: added shared helper _model_switch_steps(command) (line ~283, honors a caller-resolved command, defaults to /model opus via _DEFAULT_MODEL_SWITCH_COMMAND); _at_wedge's NO_HEADROOM row now calls it instead of hardcoding the literal; _at_idle gets a new NO_HEADROOM row that types the same /model <target> + confirm pair straight (no wedge to flush) but ONLY when input_field.kind is EMPTY (verified-field precondition -- guards an idle pane mid-typed draft), else defers (returns ()); _at_working gets an explicit NO_HEADROOM branch documenting the pre-existing refusal (law 2, already fell through to return () before this change -- no behavior change there, just made auditable). scripts/detectors/model-fallback.py: pane_actuate.act(...) call now passes command=f'/model {target}' so the configured CLAUDE_PLUGIN_OPTION_MODEL_FALLBACK_TARGET reaches the keystroke instead of the policy table's hardcoded default (closes the 'hardcodes /model opus' adjacent defect from the prior report). Tests added/updated in tests/test_pane_policy.py: test_idle_no_headroom_switches_model_when_field_is_empty, test_idle_no_headroom_honors_a_configured_target, test_idle_no_headroom_defers_over_unsubmitted_text (fixture synthetic-idle-text-typed-bypass-off.txt), test_working_no_headroom_types_nothing, test_retry_wedge_no_headroom_honors_a_configured_target; removed NO_HEADROOM from test_idle_non_cron_dead_events_do_nothing's parametrized tuple (no longer a no-op case). tests/test_model_fallback.py: _wire_acting_detector now also captures the command= kwarg into called['command']; added test_no_headroom_passes_the_configured_target_as_command. Gates on all 4 owned files: ruff/mypy/pyright all clean, pytest tests/test_pane_policy.py tests/test_model_fallback.py tests/test_pane_actuate.py -q = 102 passed. Box 3 (live no-headroom episode in pane-policy.log) still open -- unchanged, live-observation only. Report: reports/board-drain/20260917_impl-8P4BNY5J-followup.md
2026-09-17T06:10:37+02:00 — review-fork disclosure: fixed command-is-None vs truthiness in _model_switch_steps (an empty-string command was silently coalesced to the default -- now only None triggers it), re-gated clean, 102 passed. Two accepted-not-fixed gaps disclosed in the report: (a) idle+non-empty-field defers forever with no escape signal if the human abandoned the draft (livelock, not a retry) -- would need a pane_state.py signal, out of scope; (b) AWAITING_USER/UNKNOWN panes still never get the NO_HEADROOM switch, unchanged pre-existing behavior. Full disposition: reports/board-drain/20260917_impl-8P4BNY5J-followup.md 'Adversarial review' section.
2026-09-17T07:05:00+0200 — hygiene correction: an idle pane holding unsubmitted text (pane_policy._at_idle, scripts/lib/pane_policy.py:323-334, NO_HEADROOM row) is NOT reliable evidence of a human mid-draft -- in practice it is usually the janitor's own unverified leftover keystrokes from a prior send. The indefinite defer this row applies is a KNOWN LIVELOCK on that population, tracked separately by TRDD-FKY3NXB8 (filed 2026-09-17, backburner). Also noted: AWAITING_USER and UNKNOWN panes get NO keystroke at all on NO_HEADROOM (scripts/lib/pane_policy.py:411-433 falls through to bare return () for both) -- an accepted silent no-op, out of this card's scope, also referenced by FKY3NXB8.
2026-09-17T (IMPLEMENTER) — the Enter-confirm bug on the Event.NO_HEADROOM path is now closed at the root: model-fallback.py's pane_actuate.act() call passes command_plan=fleet_inject.build_command_plan(...) as the fallback build_step_plan needs to resolve the bare Enter step, so the model-switch menu is now actually confirmed (outcome DONE, not FAILED). tests/test_pane_actuate.py's pinned outcome2.status flipped FAILED->DONE plus one new direct test (test_idle_no_headroom_enter_step_is_fired_through_the_fake_terminal). Also unblocks TRDD-FKY3NXB8 (own-leftover /model text clearing) which built on the same call site.
2026-09-17T07:41:25+0200 — VERIFY pass: pane_actuate.build_step_plan's `fallback` is dual-role (a literal fallback command plan for a command step whose rebuild is impossible, AND -- only when `submit_ref` is None -- the Enter step's channel reference). model-fallback.py's fix worked but leaned on the secondary role; now passes the same built plan as BOTH command_plan= (fallback's documented job) and submit_ref= (the API's dedicated Enter-reference parameter, matching daemon.py's OWN_COMMAND_UNSUBMITTED submit_ref=plan usage). ruff/mypy/pyright clean; 108 tests pass (test_model_fallback.py + test_pane_actuate.py + test_pane_policy.py).
2026-09-17T08:33:29+0200 — implementation-commits += 9c5c8fb2 — BUNDLED commit (git-lock guard on the LOCAL-design probes, pane-test narrowing, rules trim); see its subject.
