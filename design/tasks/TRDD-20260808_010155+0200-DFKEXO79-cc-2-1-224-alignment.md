---
trdd-id: DFKEXO79
title: Align the janitor to Claude Code 2.1.224 — cwd-keyed CLI fleet enumerator first
column: dev
created: 2026-08-08T01:01:55+0200
updated: 2026-08-08T05:50:11+0200
current-owner: session-35e1e917
task-type: feature
relevant-rules: []
---

# Align the janitor to Claude Code 2.1.224 — cwd-keyed CLI fleet enumerator first

Owner directive (2026-08-07): "big changes to claude code. study them well, and update the
codebase to align to them and take advantage of them." The AUDIT half is done and recorded
(agent-handoff 2026-08-07 + janitor#92 comments); this card is the IMPLEMENTATION half.

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative; supersedes the body) — 2026-08-08

**Component 1's PURE HALF is DONE and verified end-to-end against the real CLI.**
`scripts/lib/cli_agent_roster.py` + `tests/test_cli_agent_roster.py` (22 tests, pyright 0,
built by a delegated worker, every claim re-verified first-hand): live run enumerated 25 rows /
22 distinct cwds and found THIS repo's session by its cwd key; all three `second_view_verdict`
outcomes correct. Real schema (probed, not guessed): bare JSON array; background rows carry
`id`/`state`, interactive rows `pid`/`status`; both share `cwd`/`kind`/`sessionId`/`name`.
`fetch_agents` returns (rows, why) so a failed probe is never mistaken for an empty roster.

### ✔ DONE 2026-08-08 ~02:30 — the wiring landed same-session
Wired the second view into the fleet guardian's iTerm-ambiguity path: where
`fleet_scan.record_iterm_automation_state` observes blocked (iTerm up, osascript 0), call
`cli_agent_roster.fetch_agents()` and fold `second_view_verdict` into the flag payload so the
alarm can finally say "blocked-not-empty" vs "consistent-empty". Done in-session (the two touched functions were freshest here): gather_fleet probes the
CLI ONLY on the rare blocked path, the verdict rides the flag payload (a verdict change
re-alarms once), and the alarm now states which way the second view discriminated —
including a failed probe as a failed probe. 148 tests green across both suites.

### ✔ Component 2 DONE 2026-08-08 ~04:00 — and the risk is PROVEN, not latent
The truncation regime is LIVE on this host: a probe artifact (from the peer
worktree-path-truncation-bug session) sits in ~/.claude/projects at 207 chars — 200 chars
of plain-dashed path + a `-<hash6>` suffix. So the harness's scheme is OBSERVED:
truncate at 200 + hash suffix, while project_slug plain-dashes — divergence is certain for
any >200 path. No REAL project here is close (longest 132; this repo 132). Shipped: a
canary (derived slug must name an existing harness dir for this repo) + a margin tripwire
(fails at >=190 chars naming resolve-don't-rederive as the fix). The full fix when needed:
stamp the authoritative slug dir at SessionStart from the hook payload's transcript_path.

### NEXT ACTION (one step, runnable)
Component 3 decision lines (adopt/reject/defer each: subagent-cap envs, DirectoryAdded
hook, archive source pinning, 2.1.221 reload redundancy).

### Not started
Component 2 (project_slug >200 risk) · component 3 decision lines (adopt/reject/defer each).

## Verified clean — no action (do not re-audit)

No skills use `context: fork` (58 SKILL.md, 0 — confirmed independently by the ai-maestro
sweep after its own three corrections); no deprecated `Write(...)`/`Glob(...)` permission
rules; no hook `if:` uses the changed single-segment `dir/**` form.

## Component 1 (build FIRST): cwd-keyed `claude agents --json` fleet enumerator

Measured on this host: the CLI works from a PLAIN SHELL (exit 0; `cwd`, `pid`, `state`,
`sessionId`) — so the DETACHED DAEMON can call it, with NO Automation grant. A peer measured
it enumerating 24 sessions vs ListAgents' 18, including one written off as unreachable, and
delivery to the extra rows works. Design constraints, all measured:

- **Key on `cwd` (+ `pid`), never `name`** — name is a mutable display string; six
  cross-session misroutes in one day were all name-drift. `fleet_scan` already thinks in
  project roots, so the adapter slots in without a model change.
- **Use it as the INDEPENDENT SECOND VIEW** the #92/#229 ambiguity needs: osascript's
  "0 sessions" cannot distinguish denied-vs-empty; a grant-free enumeration of the same
  population can. Report both views; disagreement IS the signal.
- **Does NOT solve delivery to a hard-wedged pane** — SendMessage enqueues to an inbox a
  frozen session may never drain (UNTESTED). ESC injection and the grant keep that rung.
  Do not let this component quietly claim the rescue path.

## Component 2: latent >200-char path-slug risk

2.1.224 fixed long-path collisions under a shared sanitized prefix. `memory_scopes.project_slug`
re-derives the slug itself (non-alnum → `-`); longest slug on this host is 132 chars, so
latent. Fix shape: RESOLVE the slug from the harness where possible, don't re-derive.

## Component 3 (evaluate, may be no-ops): smaller take-advantage items

Removed 200-subagent cap + `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`/`_SPAWN_DEPTH` (memory
chores fan out); `DirectoryAdded` hook; `archive` plugin source with SHA-256 pinning
(supply-chain interest); 2.1.221 immediate plugin activation possibly making parts of the
`[janitor-reload]` machinery redundant — MEASURE before deleting anything there.

## Acceptance

- [ ] Daemon-side enumerator lib (pure parse + classify, cwd-keyed) with tests
- [ ] Wired as the second view in the fleet guardian's iTerm-ambiguity path (#92)
- [ ] project_slug resolution hardened or explicitly risk-accepted with a test pinning 132<200
- [ ] Component-3 items each get a decision line (adopt/reject/defer) — no silent drops

## Approval log
