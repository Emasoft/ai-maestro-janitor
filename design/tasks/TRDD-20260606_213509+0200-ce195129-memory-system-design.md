---
trdd-id: ce195129-5fe9-4c0c-bc92-ebc2d4f6435d
title: Memory system — iterative design + measurement loop (markdown KB + memgrep recall)
column: dev
created: 2026-06-06T21:35:09+0200
updated: 2026-06-06T21:38:30+0200
current-owner: janitor-dev-session
assignee: janitor-dev-session
priority: 1
severity: MEDIUM
effort: L
labels: [memory-system, memgrep, recall, skill-design, measurement]
task-type: feature
parent-trdd: null
relevant-rules: []
release-via: none
delivery: direct-push
target-branch: main
test-requirements: [unit, integration]
runtime-targets: [macos, linux]
external-refs: []
---

# TRDD-ce195129 — Memory system design + measurement loop

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-06-06

**Goal (USER, /goal 2026-06-06):** iterate with the dual-test method and STEER the design of the
markdown memory system, improving it and MEASURING improvements against five criteria:
1. **Faithfulness** of recalled data (matches source-of-truth, undistorted).
2. **Correctness** of recalled data.
3. **Efficiency** of the retrieval tools (latency / token cost).
4. **Agent-usability** of the retrieval tools (can an agent drive them right from minimal text?).
5. **Skill brevity** — "the shorter the skill text to learn using it IN FULL, the better designed."

**Method (recorded as the standing practice):** the dual-test evaluation —
`~/.claude/.../memory/feedback_memory_dual_test_evaluation.md`. Run BOTH cold-recall (query from
the QUESTION/symptom vocabulary only) and write-then-recall (author then retrieve); judge BOTH the
agent's search strategy AND the system's retrieval; index notes by question/symptom, not answer
jargon; only ship a tool fix a cold-start (uncontaminated) query justifies.

**Components & status:**
- QUERY ENGINE — `memgrep` (TRDD-d151fe52): grep + markdown-structural filters + boolean `--where`
  (incl. link semijoin) + index/links/fact subcommands. Essentially built; 41 tests, clippy clean.
  SIGPIPE broken-pipe fix shipped (commit 38f5905) — surfaced by dogfooding recall.
- SKILL (teaching text) — `tools/memgrep/SKILL.md`. THIS is the artifact criterion-5 measures.
- KB CONTENT — the symptom-indexed `memory/*.md` notes. Seeded: keychain-arch + dual-test method.
  Gap: breadth.
- RECALL TRIGGER — a SessionStart/on-demand step running frontmatter-gated symptom queries. NOT
  built yet.

**NEXT ACTION:** continue the measure→improve→measure loop on `tools/memgrep/SKILL.md` (shorten
further without losing completeness), then build the recall trigger, then broaden the KB. Each
iteration logs its metrics below.

**Load-bearing finding (verified, not asserted):** the FLAG surface and the `--where` predicate
surface are ISOMORPHIC — every `--flag v` equals the predicate `flag "v"` (negatives via `not`);
the only `--where`-only predicates are the 5 file-level ones (path/name/fm/links-to/linked-from).
Verified 2026-06-06 by 4 equivalence counts (`--heading --no-code` == `--where 'heading and not
code'`, etc., all matched). ⇒ teach `--where` by DERIVATION from the flags, never re-enumerate —
shorter AND truthful. This is the central skill-brevity lever.

## Measurement log

| Iter | Change | Faithfulness | Skill size | Notes |
|---|---|---|---|---|
| 1 | SKILL.md v1 (anchored on "rg for markdown" + 5 rules + structural/where/subcmd/recall) | baseline | **263 w / 19 L** | `--where` (65w) re-listed predicates already taught as flags — double teaching surface. |
| 2 | Rewrite `--where` as derivation (`each --flag v ≡ predicate flag "v"`) after VERIFYING the flag↔predicate isomorphism with 4 real equivalence counts | verified ✓ | **250 w / 19 L** (−13w) | Bigger win than word delta: predicate set now DERIVED, not memorized. |
| 3 | Validate + quantify the skill's step-2 PRECISION recipe (`fm.description`-gated recall vs plain `-i`) | precision 2/2, recall 2/2 ✓ | n/a (system measure) | symptom `rotator`: `-i`=**6 files** (noisy) → `fm.description "rotator"`=**2** (exactly the notes ABOUT it). Noise 4→0. Also retro-confirms the dropped `[`-glob "fix" was right — `parse_frontmatter` extracts `description` fine; the earlier `[Rr]otat` miss was the contamination artifact, not a system bug. |

## Open iteration backlog (measure each)
- Shorten SKILL.md further: can the structural-flags table compress (group by value-type) without
  losing completeness? Measure words + a cold-use test (drive a recall using ONLY the skill).
- Build the RECALL TRIGGER (SessionStart hook or skill): on a task prompt, run
  `memgrep -l --where 'fm.description "<symptom>"' <memdir>` + surface `memory-index.md`. Measure
  recall precision (right note in top-k) on held-out symptom queries.
- Broaden KB content with symptom-indexed descriptions; re-measure cold-recall precision/noise.
- Efficiency: time recall over a large synthetic KB (1k notes); confirm sub-second + paths-not-bodies.

## Why this TRDD exists
The USER set a standing /goal to iteratively design + measure the memory system. This TRDD is the
durable measurement log + design-decision record so the loop survives compaction and sessions
(memgrep's own build log stays in TRDD-d151fe52; this tracks the SYSTEM + the 5-criteria scoring).
